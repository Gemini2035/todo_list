import { Component, ReactNode } from 'react';

class SearchInput extends Component {
    render(): ReactNode {
        return (
            <div>111</div>
        )
    }
}

export default SearchInput;